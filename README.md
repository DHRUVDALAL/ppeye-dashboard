# PPEye Dashboard

This is the AI-powered PPE violation detection dashboard.  
Built using Streamlit + YOLOv11.

## To run locally:

1. Install dependencies:
   pip install -r requirements.txt

2. Start dashboard:
   streamlit run dashboard.py

3. Analytics page is available via sidebar or:
   http://localhost:8501/pages/analytics
